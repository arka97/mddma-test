## Goal
Close the last GEO/AEO gap so the homepage serves real body content in initial HTML, and update the Organization structured-data logo to use a PNG asset.

## Plan
1. Make homepage prerender deterministic
   - Update the prerender flow so `/` is generated by the same script-driven path as the public subroutes, instead of relying on Vite’s default root HTML.
   - Keep the current prerendered homepage copy, nav, metadata, and JSON-LD intact unless a small cleanup is needed.

2. Unify static document generation for all public authority routes
   - Refactor the prerender script so `/`, `/about`, `/membership`, `/apply`, `/install`, `/circulars`, and `/contact` all come from one route manifest and one HTML renderer.
   - Ensure the generated root document still carries the Vite asset tags needed for hydration.

3. Fix structured-data logo format
   - Replace the Organization `logo` value from the SVG path to the existing PNG asset (`/icon-192.png` or a larger PNG if already available and more appropriate).
   - Apply the same change anywhere else the sitewide Organization schema is emitted so the source stays consistent.

4. Validate output at the artifact level
   - Rebuild locally and verify `dist/index.html` contains non-empty `<body><div id="root">…</div></body>` content for `/`.
   - Verify the homepage HTML still includes Organization/WebSite/FAQPage JSON-LD and that the Organization logo points to PNG.
   - Spot-check one or two public route files to confirm the shared prerender flow still works.

5. Mark related SEO findings fixed if applicable
   - If there are matching SEO findings for missing initial body content or logo/schema formatting, mark them fixed after the code change.

## Technical details
- Likely change surface:
  - `scripts/prerender-public.ts`
  - `index.html`
  - possibly `package.json` only if the build entry needs a small adjustment
- Current repo state already proves the prerender content can be generated; the remaining weakness is that the live root is not guaranteed to use the same generated document as the subroutes.
- The PNG asset already exists in `public/icon-192.png`, so no new asset should be required unless a larger logo PNG is preferred.

## Done when
- `dist/index.html` contains meaningful homepage body content inside `#root`.
- Homepage structured data uses a PNG logo URL.
- Public prerendered routes continue to emit static HTML with body content and JSON-LD.